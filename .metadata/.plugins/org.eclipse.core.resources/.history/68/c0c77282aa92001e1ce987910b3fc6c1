package stringExercicios;

public class tamanhoString {
	public static void main(String[] args) {
		String s = "abc";
		int tam = s.length();

		System.out.println(tam);
	}
}

/*
 * O tamanho de uma string, ou seja, o número de caracteres pode ser obtido pelo
 * método length. Este método retorna um número inteiro. 
 * 
 * String s = “abc”; int
 * tam = s.length( ); 
 * 
 * 3 O código acima retorna 3, já que a string s possui 3
 * caracteres.
 */
