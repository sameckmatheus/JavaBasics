package fundamentos;
import java.util.Scanner;

public class ex013 {
    public static void main(String[] args) {
        Scanner entrada = new Scanner(System.in);

        System.out.println("Responda às perguntas com 'sim' ou 'nao'.");
        
        System.out.print("Telefonou para a vítima? ");
        boolean telefonou = entrada.nextLine().equalsIgnoreCase("sim");

        System.out.print("Esteve no local do crime? ");
        boolean esteveNoLocal = entrada.nextLine().equalsIgnoreCase("sim");

        System.out.print("Mora perto da vítima? ");
        boolean moraPerto = entrada.nextLine().equalsIgnoreCase("sim");

        System.out.print("Devia para a vítima? ");
        boolean deviaParaVitima = entrada.nextLine().equalsIgnoreCase("sim");

        System.out.print("Já trabalhou com a vítima? ");
        boolean trabalhouComVitima = entrada.nextLine().equalsIgnoreCase("sim");

        int respostasPositivas = contadorRespostas(telefonou, esteveNoLocal, moraPerto, deviaParaVitima, trabalhouComVitima);

        if (respostasPositivas == 2) {
            System.out.println("Suspeita");
        } else if (respostasPositivas >= 3 && respostasPositivas <= 4) {
            System.out.println("Cúmplice");
        } else if (respostasPositivas == 5) {
            System.out.println("Assassino");
        } else {
            System.out.println("Inocente!");
        }
    }

    private static int contadorRespostas(boolean... respostas) {
        int contador = 0;
        for (boolean resposta : respostas) {
            if (resposta) {
                contador++;
            }
        }
        return contador;
    }
}
